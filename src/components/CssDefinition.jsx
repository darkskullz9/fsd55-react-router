const CssDefinition = () => {
    return(
        <div>
            <h1>CSS</h1>
            <p>CSS (Cascading Style Sheets) est un langage utilisé pour décrire la présentation d&#39;une page web, comme les couleurs, la mise en page, etc.</p>
        </div>
    );
};
  
export default CssDefinition;